{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Election</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Started</th>
                <td>{{ entity.started }}</td>
            </tr>
            <tr>
                <th>Finished</th>
                <td>{{ entity.finished }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('crud_election') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('crud_election_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
